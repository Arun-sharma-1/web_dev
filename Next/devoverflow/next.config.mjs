/** @type {import('next').NextConfig} */

const nextConfig = {
    experimental: {
        // serverActions: true,
        // mdxRs: true,
        // serverComponentsExteranlPackages: ["mongoose"],
    },
};


export default nextConfig;
